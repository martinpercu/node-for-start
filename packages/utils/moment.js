const moment = require('moment');

let rightNow = moment();

console.log(rightNow);
console.log(rightNow.toString());
console.log(rightNow.format('YYYY / MM / DD  ==> HH:mm:ss'));
console.log(rightNow);
